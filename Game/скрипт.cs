using UnityEngine;

public class скрипт : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created\
    public GameObject TEST;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
